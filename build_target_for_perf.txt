#!/bin/bash

set -e

BUILD_DIR=build-perf
APP=./ton_app   # adapte ici

echo "== Build (perf mode) =="

cmake -S . -B $BUILD_DIR -DCMAKE_BUILD_TYPE=RelWithDebInfo
cmake --build $BUILD_DIR -j

echo "== Run perf =="

cd $BUILD_DIR

perf record -g $APP

echo "== Report =="

perf report
